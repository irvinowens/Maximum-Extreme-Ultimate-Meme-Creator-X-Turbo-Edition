buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.4'
    }
}
apply plugin: 'android'

dependencies {
    compile files('../../../../MaximumUltimateMemeCreatorXTurbo/libs/libspen23.jar')
    compile files('../../../../MaximumUltimateMemeCreatorXTurbo/libs/libspen23_multiwindow.jar')
    compile files('../../../../MaximumUltimateMemeCreatorXTurbo/libs/javacpp.jar')
    compile files('../../../../MaximumUltimateMemeCreatorXTurbo/libs/javacv.jar')
}

android {
    compileSdkVersion 17
    buildToolsVersion "17"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 17
    }
    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['src']
            resources.srcDirs = ['src']
            aidl.srcDirs = ['src']
            renderscript.srcDirs = ['src']
            res.srcDirs = ['res']
            assets.srcDirs = ['assets']
        }

        instrumentTest.setRoot('tests')
    }
}
